-Write Forward Pass
    -Basic Forward pass works
    -Test More complicated NN Module and dependencies
-Write Backward Pass
-Make so Local_Mem is not used when the variable stored has no local uses

-Train Model on naive version

-Implement Pipelined version (the pipeline may not be effecient since the nodes are only
                                connected through the server)
